//initial state
const initialState = {};

//action constants

//reducer
export default function groupsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return { ...state };
  }
}

//action creators
