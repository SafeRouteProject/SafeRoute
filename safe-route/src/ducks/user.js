//initial state
const initialState = {
  exactLocation: ""
};

//action constants

//reducer
export default function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return { ...state };
  }
}

//action creators
